version: 2
models:
  - name: bro_sfb_cnfp
    description: "Bronze SFB — Cadastro Nacional de Florestas Públicas."
    config:
      tags: ['bronze']
    columns:
      - name: geometry
        data_type: geometry
        description: "MULTIPOLYGON EPSG:4326 — saida do clean_geometry."
        tests:
          - not_null
