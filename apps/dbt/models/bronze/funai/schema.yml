version: 2
models:
  - name: bro_funai_tis_poligonais
    description: "Bronze FUNAI — Terras Indígenas poligonais."
    config:
      tags: ['bronze']
    columns:
      - name: geometry
        data_type: geometry
        description: "MULTIPOLYGON EPSG:4326 — saida do clean_geometry."
        tests:
          - not_null
