version: 2
models:
  - name: bro_nasa_firms_viirs_noaa20
    description: "Bronze NASA FIRMS VIIRS NOAA-20 — clean_point_geometry(rawdata, 4326)."
    config:
      tags: ['bronze']
    columns:
      - name: geometry
        data_type: geometry
        description: "POINT EPSG:4326 — saida do clean_point_geometry."
        tests:
          - not_null
